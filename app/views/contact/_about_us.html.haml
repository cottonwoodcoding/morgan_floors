.col-sm-4.col-lg-4
  .panel.no-background
    .panel-heading
      %h3
        %i.icon-pushpin.main-color
        .font-m Information
    .panel-body
      %address
        %div.contact_name
          .big-logo
            %img{src: image_path('harmorg-flooring-div_logo-inverted.png')}
          %br
          = render(partial: 'addresses') if addresses
          = render(partial: 'phones') if phones
